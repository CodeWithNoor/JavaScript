// intro to array --> ordered of collection items --> refrence data type
// array stored --> string, numbers, null , undefined etc... 

// how to create array;

let names = ["Abbas", "Noor", "Zain", "Aleena"];
console.log(names);

let fruits = ["Apple", "Banana", "Orange", "Grapes"]
fruits[1] = "Phalsy"; // --> change the 1 index 
console.log(fruits);

// check the array or not
console.log(Array.isArray(fruits));

// indexing of array